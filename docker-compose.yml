services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./graphs:/app/graphs
      - ./uploads:/app/uploads
      - ./config.json:/app/config.json
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
    user: "0:0"
